# Application URL - The base URL of the deployed application
# Can be fetched from CloudFormation stack outputs or set manually
BASE_URL=https://your-cloudfront-distribution.cloudfront.net

# Cognito Configuration
# These can be fetched from CloudFormation stack outputs
COGNITO_USER_POOL_ID=eu-west-2_xxxxxxxxx
COGNITO_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx
# Test Script Client ID - This client allows USER_PASSWORD_AUTH flow
TEST_SCRIPT_USER_POOL_CLIENT_ID=xxxxxxxxxxxxxxxxxxxxxxxxxx

# Test User Credentials
TEST_USER_EMAIL=test.user@example.com
TEST_USER_PASSWORD=your-test-user-password

# Admin User Credentials
ADMIN_USER_EMAIL=admin.user@example.com
ADMIN_USER_PASSWORD=your-admin-user-password

# AWS Configuration
AWS_REGION=eu-west-2
AWS_PROFILE=streaming

# API Endpoint (optional, defaults to BASE_URL if not set)
# API_ENDPOINT=https://your-api-gateway-url.execute-api.eu-west-2.amazonaws.com/prod

